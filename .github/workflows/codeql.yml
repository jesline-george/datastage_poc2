detectors:
  - name: DataStageConnectionCredentials
    keywords:
      - password
      - server
    regex:
      password: '(?i)password\s*=\s*"?([^"\s;]+)"?'
      server: '(?i)(?:server|host|hostname)\s*=\s*"?([^"\s;]+)"?'
